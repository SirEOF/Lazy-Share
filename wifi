#!/bin/bash

# Change the name to the name of server hotspot
#sudo ifconfig wlan0 up
#sudo nmcli device wifi list > /dev/null
#sudo nmcli device wifi connect hii password qwertyuiop > /dev/null
echo "Connected"
